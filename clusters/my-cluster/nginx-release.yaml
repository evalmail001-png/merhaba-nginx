apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: flux-nginx
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: nginx
      version: "23.0.5"
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
